import NftCard from "../_components/NftCard";

const Nfts = () => {
  return (
    <div className="flex gap-12 flex-wrap justify-between w-full">
      <NftCard />
    </div>
  );
};

export default Nfts;
